name: payment-api
namespace: payment
apiKey:
  # When true, API_KEY is sourced from a Secret via secretKeyRef (correct pattern).
  # When false, it's rendered as a plain env value (the anti-pattern).
  useSecretRef: false
  plainValue: sk-live-abcd1234efgh5678
